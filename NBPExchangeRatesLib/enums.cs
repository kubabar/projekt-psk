namespace NBPExchangeRatesLib
{
    public enum DateOption
    {
        Latest,
        SpecificDate,
        DateRange
    }

    public enum FetchFormat
    {
        XML,
        JSON
    }

    public enum FileFormat
    {
        txt,
        csv
    }
}
